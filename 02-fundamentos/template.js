const nombre = "JHUDER";
const apellido = "PALOMIINO MATIAS"

const normal = nombre + ' ' + apellido;
const template = `${nombre} ${apellido}`

if (normal == template) {
    console.log('SON IGUALES'); 
}

console.log(normal);
console.log(template);